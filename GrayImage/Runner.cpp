// GrayImage.cpp : This file contains the 'main' function. Program execution begins and ends there.
//
#include <iostream>
#include "GrayImage.h"
#include <cmath>
using namespace std;

int main()
{
    /*GrayImage image(1000, 1000);
    for (int y = 0; y < image.getHeight(); y++)
    {
        for (int x = 0; x < image.getWidth(); x++)
        {
            image.setPixel(y, x, (uint8_t)(tan(x / 20.0) * tan(y / 20.0) * 255.));
        }
    }*/
    
    return 0;
}

